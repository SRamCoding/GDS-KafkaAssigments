# =====================================================
# ⚠️ IMPORTANTE:
# ESTE ES UN ARCHIVO DE EJEMPLO (.env.example).
# DEBES COPIARLO COMO .env Y PONER TUS CREDENCIALES REALES.
# =====================================================

# 🔹 Kafka / Confluent Cloud
# Servidor bootstrap de Kafka en Confluent Cloud
KAFKA_BOOTSTRAP=pkc-<REGION>.confluent.cloud:9092
# Clave de API que obtienes en Confluent Cloud
KAFKA_API_KEY=your_api_key_here
# Secreto de la API (como una contraseña)
KAFKA_API_SECRET=your_api_secret_here

# 🔹 Schema Registry
# URL de tu Schema Registry en Confluent
SCHEMA_REGISTRY_URL=https://psrc-<REGION>.confluent.cloud
# Clave del Schema Registry (también se genera en Confluent Cloud)
SCHEMA_REGISTRY_KEY=your_schema_key
# Secreto del Schema Registry
SCHEMA_REGISTRY_SECRET=your_schema_secret

# 🔹 Topic / Schema
# Nombre del topic de Kafka donde se producirán/consumirán los mensajes
KAFKA_TOPIC=changes_product_table
# Nombre del subject en el Schema Registry asociado al topic
SCHEMA_SUBJECT=changes_product_table-value

# 🔹 MySQL (local o docker-compose)
# Si usas Docker Compose y un servicio llamado "mysql", host será "mysql"
# Si corres MySQL en tu máquina local, host será "localhost"
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=rootpass
MYSQL_DATABASE=assKafka_BuyOnline_Company # ESTO NO LO TOQUES, DÉJALO ASÍ